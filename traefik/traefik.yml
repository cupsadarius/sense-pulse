providers:
  docker:
    exposedByDefault: false
    # Host network mode: Traefik connects to containers via Docker network
    # but listens on host interfaces (sees Tailscale)
    network: sense-pulse_internal

entryPoints:
  web:
    address: ":80"

api:
  dashboard: false

log:
  level: WARN
